import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  // Le port se configure depuis le script de démarrage : "next dev -p 3001"
};

export default nextConfig;
